# DEPRECATED — use ci.yml. Kept for branch compatibility; runs only on manual dispatch.
name: Build (deprecated — see CI)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "build.yml is deprecated — use .github/workflows/ci.yml"
