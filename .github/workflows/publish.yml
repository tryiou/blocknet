# DEPRECATED — use release.yml (Guix, ubuntu 24.04, actions v4/v6).
# This file is kept as workflow_dispatch-only shim for old tag reproducibility.
name: Publish (deprecated — see Release)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "publish.yml is deprecated — use .github/workflows/release.yml (Guix)"
