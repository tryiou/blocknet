--- a/Configurations/10-main.conf
+++ b/Configurations/10-main.conf
@@ -1746,6 +1746,19 @@
         shared_rcflag    => "--target=pe-x86-64",
         multilib         => "64",
     },
+    # Windows ARM64 (aarch64-w64-mingw32); mirrors the MSYS2 mingwarm64
+    # target (upstream OpenSSL only carries mingw/ming64 here).
+    "mingwarm64" => {
+        inherit_from     => [ "mingw-common" ],
+        cflags           => "",
+        sys_id           => "MINGWARM64",
+        bn_ops           => add("SIXTY_FOUR_BIT"),
+        asm_arch         => 'aarch64',
+        uplink_arch      => 'armv8',
+        perlasm_scheme   => "win64",
+        shared_rcflag    => "",
+        multilib         => "-arm64",
+    },
 
 #### UEFI
     "UEFI" => {
